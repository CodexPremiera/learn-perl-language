#!/usr/bin/perl
# This is the shebang line that tells the system to use Perl to run this script.

use strict;     # enforce strict variable declaration
use warnings;   # enable warnings to catch common mistakes

# Print "Hello, World!" to the console
print "Hello, World!
This is my first PERL code.
"

# Comments are single-lines that starts with `#`

# To run perl program, type `perl filename.pl` in the terminal